# AGENTS.md — trellis2 (ggml port of TRELLIS.2)

本文件定义**默认推理链路与端到端（e2e）验收标准**。任何改动推理数值路径
（trellis2.cpp / ggml 补丁 / 模型量化策略）后，必须按本文重新验证。

## 1. 默认推理 / 端到端链路（golden chain）

**金标准链路 = CUDA + f16 + quality 512 + seed 0 + steps 12 + PBR 纹理**，
即产出 `e2e_out/golden_cuda_f16.glb`（~58 MB，1,526,056 verts / 3,173,502 tris）的链路：

```sh
TRELLIS2_DEVICE=cuda build-cuda/examples/t2_generate \
  --models-dir models \
  --input assets/example_image/T.png \
  --out-mesh out.t2mesh --out-glb out.glb \
  --quantization f16 --quality 512 --seed 0 --steps 12 \
  --save-grid out.t2grid
```

要点：
- `--quantization` 默认已改为 **f16**（`t2_generate` / `t2_texture` 源码内默认值）。
  q8 只量化 `ss_dec`（`keep_f16` 名单覆盖 dino / ss_flow / slat_flow /
  tex_slat_flow / shape_dec / tex_dec / shape_enc），仅按需使用。
  **2026-08-30 实验定论**：三 VAE 阶段的 q8 均致命（CUDA+Vulkan 双后端复现）——
  shape_dec q8 → level-0 subdivision logits 全体翻负（"no children at level
  0"，它们本就贴着零点分布）；tex_dec q8 → PBR 材质饱和塌缩；shape_enc q8 →
  条件误差被 12 步纹理采样器混沌放大成同样的饱和塌缩。权重 rel-L2 ~5-7e-3
  是正常 Q8_0 噪声（f16 的 ~25 倍）。另：ss_dec 的 dense conv3d 权重布局
  [3,3,3,N] 不满足 Q8 块对齐（dims[0]=3），其 q8 gguf 是 f16 的逐字节克隆——
  **当前 q8 模式实际体积收益为零**（除 rmbg）。
- **不要设置** `GGML_CUDA_CUBLAS_COMPUTE_TYPE` / `TRELLIS2_SDPA_EXACT` /
  `TRELLIS2_SDPA_FLASH` / `NVIDIA_TF32_OVERRIDE`（代码内已做正确默认）。
- 一键脚本：`scripts/run_pbr_e2e.sh`（默认 `QUANT=f16 SEED=0 QUALITY=512`）。

### 验收门槛（对金标准输入 T.png）

| 指标 | 门槛 |
|---|---|
| CUDA 网格 vs `dumps/golden_grid_cuda.t2grid`（Jaccard） | ≥ 0.999 |
| CUDA q8 vs CUDA f16（Jaccard） | = 1.0000 |
| CPU 网格 vs `dumps/golden_grid_cpu.t2grid` | ≥ 0.999 |
| CUDA 1024 级联 | 正常出网格（≥6M vox，不报 "no children"） |
| 退出码 | 0 |

网格比较工具：`python3 scripts/t2grid_jaccard.py A.t2grid B.t2grid [--thr 0.999]`
（.t2grid = `T2GRID01` 头 + f32 feats + i32 坐标数组，按体素集合算 Jaccard）。
两份金标准参考网格为"好时代"输出，保存在 `dumps/`（git 忽略，勿删）。

## 2. 数值正确性前提（"好时代"修复，禁止回退）

1. **SDPA 走 exact 路径**：`sdpa_auto` 默认 exact（≤12 GiB 分数矩阵），
   超限按 Q 维分块 exact（softmax 逐行独立，数学等价）；仅
   `TRELLIS2_SDPA_FLASH=1` 才用 flash。CUDA/CPU 的 flash kernel 均为
   f16 近似（~3e-3 rel-L2/forward），会被混沌采样器放大成完全不同的体素集。
2. **所有 `lin()` / 稀疏 conv GEMM / SDPA matmul 强制 `GGML_PREC_F32`**。
3. **TF32 关闭**：`init_best_backend` 里 `setenv("NVIDIA_TF32_OVERRIDE","0",0)`。
4. **ggml 补丁**（`patches/ggml-trellis.patch`，CMake configure 时自动 apply 到
   build/_deps 物化树）：CUDA `mmvf/mmf` f16 kernel 忽略 PREC_F32，补丁让其
   旁路到 cuBLAS F32 路径。删补丁 / 升级 ggml 后必须回归本文件的验收门槛。
5. 详见 `docs/VERIFICATION.md`（含失败模式清单）。

## 3. 管线阶段与模块

`trellis2_generate_mesh` 阶段（t2_generate 进度回调中的 stage 号）：

| stage | 模块 | 模型 | 说明 |
|---|---|---|---|
| 0 | preprocess | rmbg_f16 | 抠图 + 裁剪归一化 |
| 1 | dino_cond | dino_f16 (ViT-L/16) | 1029 tokens × 1024 条件 |
| 2 | ss_flow | ss_flow_f16 | 16³ 结构 latent，12 步 CFG 扩散 |
| 3 | ss_dec | ss_dec | 64³ occupancy（q8 模式下唯一量化项） |
| 4 | slat_flow | slat_flow_512_f16 | 512 级稀疏 latent，12 步 CFG |
| 5 | shape_dec | shape_dec_f16 | 512³ dual-grid 特征 |
| 6 | dual grid + marching | mesh_export.cpp | 顶点/三角形提取 |
| 10 | tex_flow | shape_enc + tex_slat_flow_512 + tex_dec | PBR 属性场，12 步 |
| — | GLB 导出 | mesh_export | UV unwrap：CPU 法向锥聚类（cone_cluster，CuMesh 算法的零依赖移植）+ xatlas 参数化；纹理烘焙 + GLB 序列化（~22 s，零 libtorch） |

backend 选择：`TRELLIS2_DEVICE=cuda|vulkan|cpu`，默认自动选最优。

## 4. 性能基线（RTX 4090 24 GB，quality=512，seed=0，steps=12，含 PBR 纹理）

| 组 | 总耗时 | ss_flow | slat_flow | tex_flow | 每 step（2/4/10） | verts | GLB |
|---|---|---|---|---|---|---|---|
| cuda f16 | **82.6 s** | 20.7 s | 17.7 s | 18.3 s | 1.72 / 1.48 / 1.20 s | 1,526,056 | 58.4 MB |
| cuda q8  | **82.5 s** | 20.6 s | 17.7 s | 18.4 s | 同上 | 1,526,056 | 58.4 MB |
| vulkan f16 | **57.6 s** | 8.9 s | 8.4 s | 13.4 s | 0.74 / 0.70 / 0.76 s | 1,567,973 | 51.1 MB |
| vulkan q8  | **59.0 s** | 8.8 s | 8.3 s | 14.0 s | 同上 | 1,567,973 | 51.1 MB |

其余阶段（两组一致量级）：模型加载 ~3.2 s、preprocess ~0.05 s、dino ~0.1 s、
ss_dec ~2.9 s、shape_dec ~3.5 s、dual grid/mesh ~2 s、GLB 烘焙/序列化 ~14 s。
四组数据中扩散阶段耗时稳定（step 间方差小，首个 step 含图编译略高）。

说明：CUDA 慢于 Vulkan 是**有意为之**——CUDA 走精确 F32 cuBLAS（金标准语义），
Vulkan 用 coopmat2 f16 快速管线（见 §5，不同 lineage）。

## 5. 稳定性与 lineage 语义

采样器是混沌系统：数值差异经 12 步 CFG 放大。2026-08-29 的控制变量实验
修正了旧结论（“两条 f32-语义 lineage 互差 ~0.35”是错的）：

- **cuda-f32（--quantization f32）↔ cpu-f16 = 0.974**：f32 累加语义下跨实现
  对齐**可达**；cpu 的 f16 matmul 本就 f32 累加，语义≈f32。
- **cuda-f32 ↔ cuda-f16 = 0.347**：旧“双 lineage ~0.35”的主因是 CUDA-f16 链路
  自身的 f16 累加/激活噪声，而非跨实现差异。
- **后端内稳定（验收标准）**：CUDA/Vulkan/CPU 各自 q8↔f16 Jaccard = 1.0000。
- **Vulkan 默认 f32acc**（2026-08-29 起）：`init_best_backend` 检测到 Vulkan
  设备时自动 `setenv(GGML_VK_DISABLE_F16=1)`（用户显式 env 可覆盖）。此前
  coopmat2 fp16-累加使 dino cond rel-L2 ~1.5e-3、PBR 场高频噪声 +35%；修复后
  降至 ~1.8e-4 / 接近 CUDA 水平，代价是部分性能（RTX 4090 512 全链 ~75s，
  仍快于 CUDA-f16 ~83s）。
- **f32 权重模式下三后端对齐达成（2026-08-29）**：`--quantization f32` 时
  cuda↔vulkan 单步 DiT velocity rel-L2 仅 **3.5e-6**，e2e 体素 Jaccard **0.9998**；
  纹理统计三方逐位同水平（hf 0.0748/0.0745/0.0747）。`vulkan_f32.*` 已入
  `e2e_out/`。根因：Vulkan 的 f16 权重 matmul 管线（`pipeline_matmul_f16_f32`
  .f32acc）存在 ~5.4e-4 单步残差，f32 权重走 `pipeline_matmul_f32` 则纯净。
- **f16 权重模式的残差是管线固有**：逐 block taps（`TRELLIS2_SS_TAPS=dir`，
  ss/slat forward 均支持）显示三后端在 block0 就互差 ~5e-5、24 层随机游走至
  ~3e-4（cuda↔cpu 6.0e-4 甚至大于 cuda↔vulkan 5.4e-4，无缺陷 op）；混沌放大
  后是否跨阈值接近抛硬币（cpu 未跨→0.974，vulkan 跨了→0.24）。彻底消除需
  f32 权重（f32 模式）。
- **性能（RTX 4090，quality 512，有并行负载时的相对值）**：vulkan-f16(f32acc)
  ~75s < cuda-f16 ~83s < vulkan-f32 ~89s ≈ cuda-f32 ~90s。对齐交付物用
  f32 模式；追求速度且接受独立 lineage 用 f16。
- **f16/q8 模式也已对齐（2026-08-29，`patches/ggml-vulkan-f32-route.patch`）**：
  ggml-vulkan 中 `GGML_PREC_F32` 的 f16 权重 GEMM 原本走
  `pipeline_matmul_f16_f32.f32acc`，但 coopmat2 会先把激活 Y 转成 fp16（
  `y_non_contig` 分支），单步 rel-L2 ~5.4e-4。patch 修正两点：① PREC_F32 时
  Y 保持 fp32（豁免 coopmat2 转换）；② f16 权重 dequant 到 fp32 走纯 f32
  管线（f16→f32 无损，与 cuBLAS/CPU 数学等价）。单步 rel-L2 5.4e-4 →
  8.7e-5（低于 cuda↔cpu 的 6.0e-4 噪声底）；e2e：vulkan-f16↔cuda-f16 与
  vulkan-q8↔cuda-q8 均 **0.9868**，q8↔f16 仍 1.0000。性能 ~87s（与 cuda 相当）。

## 6. 构建

```sh
cmake -B build-cuda   -DGGML_CUDA=ON  -DCMAKE_CUDA_ARCHITECTURES=89 && cmake --build build-cuda -j
cmake -B build-vulkan -DGGML_VULKAN=ON                               && cmake --build build-vulkan -j
```

- ggml 子模块由 `_trellis_materialize_submodule` 在 configure 时物化到
  `<build>/_deps/third_party-ggml-<sha>-` 并 apply `patches/*.patch`；
  换 ggml commit 后 stamp 变化会自动重物化。
- `third_party/cumesh_glue` **默认 OFF**（`TRELLIS2_CUMESH=OFF`，2026-08-30 起）：
  它内部链接 conda libtorch，与"端到端零 PyTorch 运行时"冲突。默认构建的 UV
  unwrap 走 `cone_cluster`（CuMesh compute_charts 的 CPU 移植：法向锥 collapse
  + refine，Garland 堆实现，零依赖；T.png 512 聚类 3.1 s，chart 数与 GPU 原版
  同量级，纹理统计一致）；需要 GPU 原版加速时显式
  `cmake -DTRELLIS2_CUMESH=ON -DCUMESH_TORCH_DIR=...`（已显式链接
  `libtorch_python.so` + `libpython3.x.so`，否则下游链接报 `undefined
  reference to PyErr_Fetch`）。`T2GLB_XATLAS=1` 的 xatlas 自主聚类对大网格
  minutes-slow（历史上 >40 min），仅作调试/对比用途；`simple_unwrap` 仅作
  最后 fallback（6-bin 投影渲染效果差：大面积 alpha/RGB 失效）。

## 7. 已知问题

1. ~~`test_print_remesh` 失败~~ 已修复（2026-08-28）：根因并非 CuMesh 子模块
   （`print_remesh.cpp` 纯 CGAL；CuMesh 仅参与 atlas chart 聚类），而是
   `prepare_print_mesh` 的 per-vertex PBR 预览语义（closest-surface 投影，
   server `PreparePrintMesh` 依赖）与过时的测试断言 / `trellis2_capi.h`
   "geometry-only (no PBR)" 文档冲突；已将两者对齐到实现语义，ctest 通过。
2. ~~Vulkan 与 CUDA/CPU 不同 lineage~~ 大部分修复（2026-08-29）：根因是
   DINO ViT 的手写 SDPA 漏设 `GGML_PREC_F32`（`trellis2.cpp` sdpa lambda）
   + Vulkan coopmat2 fp16-累加。已修：sdpa 补 prec、Vulkan 默认 f32acc
   （见 §5）、dino cond rel-L2 从 ~1.5e-3 降至 ~1.8e-4、q8↔f16 各后端
   1.0000。残余：单步 DiT ~5.4e-4 的非 matmul shader 差异使 vulkan-f16 仍为
   独立 lineage（vs cuda-f32 ~0.24）；GLB 导出的 alpha 噪声带修复
   （snap-to-opaque + BLEND 判定修正）已落地。**跨后端对齐已全部达成（同日）**：
   `patches/ggml-vulkan-f32-route.patch`（f32 路由 + Y 保持 fp32，见 §5）使
   vulkan-f16↔cuda-f16 与 vulkan-q8↔cuda-q8 均 **0.9868**、
   vulkan-f32↔cuda-f32 **0.9998**；交付物在 e2e_out/ 同名对应组合。
3. quality 1024：CUDA 正常；**Vulkan 1024 未通过**（2026-08-29 实测：HR
   shape decode 阶段 15.6 GB 单 Vulkan buffer OOM，f16acc/f32acc 均失败，
   与数值修复无关，系 ggml gallocr 单 buffer 预算限制，待显存分块优化）。
4. CPU 后端可用于验收（f32 语义，与 cuda-f32 互验 0.974），速度仅为参考，
   不作性能基线。
